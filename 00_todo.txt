log to syslog

switch to uv shell scripts

#!/usr/bin/env -S uv run --script
#
# /// script
# requires-python = ">=3.12"
# dependencies = ["httpx"]
# ///

remove path dependecies in nzbfix

bliss.py
  - logging
  - default: only scan top level dir, --all goes deep
  - command line option --cleanup w/o flac checks
  - better logging for flac



fixtags.py
  - extra logging argument color


  Lyrics cloud, search for text, shows songs


update_lyrics.py
- upgrade to LRC only if len(lrc) > len(txt)


updated lyrics don't trigger rclone update ;(